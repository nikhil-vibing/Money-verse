#!/usr/bin/env node
// Placeholder for the content compile pipeline.
// Steps it will run:
//   1. Compile .yarn files in quests/ to .yarn.json
//   2. Validate each quest's frontmatter against QuestSchema
//   3. Validate NPC schedule conflicts within a district
//   4. Bundle into a single content.compiled.json for runtime
console.log("[content:compile] placeholder — full pipeline coming with first real quest");
